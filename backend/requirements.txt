# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
motor==3.6.0          # async MongoDB driver
pymongo>=4.9,<4.10    # Motor dependency (compatible version)
sqlalchemy==2.0.36    # PostgreSQL ORM (for payments)
asyncpg==0.30.0       # async PostgreSQL driver

# Auth & security
python-jose[cryptography]==3.3.0   # JWT
passlib[bcrypt]==1.7.4             # password hashing
bcrypt>=4.0.1,<5.0.0               # bcrypt backend for passlib (v4 for compatibility)

# Config
pydantic==2.9.2
pydantic[email]==2.9.2
pydantic-settings==2.5.2

# Utilities
python-multipart==0.0.12   # file uploads
httpx==0.27.2              # async HTTP client (for tests)

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
